# School Management System

# Data structures
usuarios ← dictionary with users credentials, roles, and names
materias ← tuple with the 7 subject names
calificaciones ← dictionary containing student grades for each subject

# INPUT
logged_in ← False
While logged_in is False:
    Get username from user
    Get password from user
    If username exists in usuarios AND password matches:
        logged_in ← True
        role ← usuarios[username]['rol']
        name ← usuarios[username]['nombre']
        Print "Welcome"
    Else:
        Print "Error"

# PROCESS AND OUTPUT
If role is 'alumno':
    approved ← empty set
    pending ← empty set
    For each materia in materias:
        grade ← calificaciones[username][materia]
        Print "materia" and "grade"
        If grade >= 8.0:
            Add materia to approved set
        Else:
            Add materia to pending set
    Print approved set
    Print pending set

Else if role is 'maestro':
    For each user in usuarios:
        If user role is 'alumno':
            Print "user" and "student name"
    Get student_to_grade from user
    For each materia in materias:
        Print materia
    Get subject_to_grade from user
    Get new_grade from user
    old_grade ← calificaciones[student_to_grade][subject_to_grade]
    Get "confirm" from user
    If confirm is "yes":
        calificaciones[student_to_grade][subject_to_grade] ← new_grade
        Print "Confirmation" and updated records
    Else:
        Print "Cancellation"

Else if role is 'coordinador':
    Print list of teachers from usuarios
    Print list of students from usuarios
    Print grades matrix header
    For each materia in materias:
        Print row with subject name and the grade of each student